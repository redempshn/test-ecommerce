import { VscAccount } from "react-icons/vsc";

const Account = () => {
  return (
    <button className="cursor-pointer" onClick={() => {}}>
      <VscAccount size={20} className="hover:fill-blue-500 transition" />
    </button>
  );
};

export default Account;
